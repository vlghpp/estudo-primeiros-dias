let condicao = true;//Define a váriavel condição como verdadeira

//OPERADORES:
// == igual | != diferente | < menor que | > maior que
// || -> disjunção (ou) && -> conjunção (e)
// === igual em tipo e valor | !== difrente o valor e o tipo


if (condicao == false) console.log("Esse if é false");//Printa no console a mensagem caso o paramentro seja falso
else console.log("Esse if é true");//Printa no console a mensagem caso o parametro seja verdadeiro